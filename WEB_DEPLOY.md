# 运行与部署

## 单人账户

首次访问选择“注册管理员”，填写用户名、至少 12 字符的密码及部署初始化码。初始化码为 `RADAR_ACCESS_TOKEN`；本机 Web 模式为数据目录中的 `access-token`。注册成功后关闭注册入口。服务器模式下，原初始化码不再具有访问数据的权限。

密码以 scrypt 加盐哈希保存；会话令牌只保存哈希。Web 登录有效期 12 小时，桌面同步登录有效期 30 天，服务重启后会话保持有效。退出撤销当前会话，修改密码撤销全部账户会话，桌面同步随后需要重新登录。

## Docker 自部署

仅使用独立容器和数据卷。先复制 `.env.example` 为 `.env` 并设置随机初始化码（至少 24 字符），例如用 `node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"` 生成。

```bash
docker compose up -d --build
docker compose logs --tail=100 radar
```

默认只绑定宿主机 `127.0.0.1:8789`。将独立 HTTPS 域名反向代理到该端口，并把 `RADAR_PUBLIC_URL` 设置成对应的 HTTPS 地址。桌面远程同步要求 HTTPS；本机测试允许 HTTP localhost/127.0.0.1。

示例反向代理 location（加入自己的独立域名配置）：

```nginx
location / {
    proxy_pass http://127.0.0.1:8789;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 200s;
    client_max_body_size 4m;
}
```

Compose 的 `restart: unless-stopped` 用于进程退出和服务器重启后的恢复；挂载数据卷保存渠道、密码哈希、登录会话及预警记录。不要让两个进程同时写同一数据目录，服务启动时会检查锁。

## 后台探测与报警

后台调度在 Node 服务中运行，不依赖网页。渠道保存有效凭据并设置大于 0 的自动探测间隔后启用，单渠道间隔优先，0 继承全局设置；两者为 0 时仅手动探测。调度每 5 秒检查一次，到期渠道最多并发 3 个。

邮件需要启用提醒、填写 SMTP 和收件人并保存。三级阈值支持每渠道独立设置；首次探测已低于阈值也会触发。跨过多级时只发当前最高级别，不补发较轻等级。发送失败保留待发送状态，下次探测重试。余额回到一级线以上后自动重新布防。

Sub2API 的 JWT/RT 续期也在后台探测期间执行；RT 本身撤销或过期时需要在上游重新获取。不要由桌面和服务器同时使用同一个会轮换的 RT。

## 桌面独立运行与同步

桌面内置 Node 服务，默认读取 `%LOCALAPPDATA%\BalanceRadar\data`，无需服务器地址或管理员登录。关闭整个桌面程序会结束其内置服务；需要全天报警时交给服务器运行，或单独启动本机 Web 服务。

在设置页的“服务器同步”中输入自己的服务器地址、用户名和密码，连接后点击同步。默认同步渠道地址、探测和调用凭据、独立阈值及自动探测设置。SMTP 邮箱配置只有勾选后才同步。Codex 配置、会话、本机文件路径、导入来源以及本机外观不上传。

首次同步合并两边独有渠道；此后根据上次同步记录合并单边变化和删除。两端同时修改同一项或删除与编辑冲突时，暂停写入并显示冲突，可明确选择保留本机值或服务器值。服务器配置修订号和本机修订号在提交时检查，避免覆盖并发编辑。网络故障或登录过期时保留本机数据；重连后可重试。

默认“已同步渠道由服务器自动探测和报警”：桌面显示服务器探测结果，手动探测也交给服务器；本机不对这些渠道重复调度或发邮件。同步是显式操作，编辑渠道配置后再次点击同步即可更新另一端。取消此选项并重新连接、同步后可由两端各自监控，但可能重复提醒或同时轮换上游 RT。

“断开并独立运行”保留本机渠道并恢复本机调度。服务器仍独立工作；完全停用服务器报警需要在服务器停用邮箱提醒或对应探测调度。

## 备份与恢复

停服务后备份整个数据目录，而不是仅复制 `config.json`。`vault-key`（或外部提供的 `RADAR_VAULT_KEY`）必须与加密配置一起保留；还应包含 `admin-account.json`、`monitor-state.json` 和桌面可选的 `server-sync.json`。备份包含敏感材料，应放在受保护的位置，不提交到 GitHub。

推荐使用同步迁移桌面渠道到服务器，避免覆盖服务器独立的管理员账户和密钥。若手工恢复，先停服务、备份现有目录，再恢复同一套配置和密钥。密码遗忘时需在服务器停服后备份并移走 `admin-account.json`，重新使用部署初始化码注册管理员，随后更新桌面连接。

## 验收范围

`pnpm test` 覆盖账户、安全边界、同步合并、后台邮件、探测转换、Sub2API 刷新、会话文件、数据库和 Codex 适配逻辑。`node scripts/verify-release.mjs` 验证两服务 HTTP 联动和本地 SMTP 真正投递。

官方 Codex 的插件安装、会话移动和 Timeline 实操需要已登录的官方 Codex 调试实例，逻辑测试不代表对应版本的真实界面已验收。付费模型真实调用、外网域名/TLS、云服务器重启和实际邮箱投递需在目标部署环境单独确认。
