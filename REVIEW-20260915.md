# Web 与桌面审查记录

日期：2026-09-15。项目：`E:\upstream-balance-tauri`。本次未执行云服务器部署。

## 实现与修复

| 项目 | 结果 |
| --- | --- |
| 首次注册 | 唯一管理员、初始化码校验、并发注册串行保护、注册后关闭入口 |
| 登录与退出 | 用户名/密码、scrypt 加盐哈希、HttpOnly Cookie、登录限流、持久化会话、退出撤销 |
| 修改密码 | 核对原密码，修改后撤销全部账户会话 |
| 桌面独立运行 | 默认连接内置本机服务，旧远程地址不阻塞启动 |
| 可选同步 | 本机设置页登录服务器，双向合并渠道、阈值、间隔；邮箱凭据显式选择同步 |
| 同步冲突 | 三方合并、删除/编辑冲突、两端修订号保护、失败后保留本机副本 |
| 敏感数据 | 同步登录凭据加密；账户密码和会话只存哈希；Codex 本机配置/路径不上传 |
| 报警责任 | 服务器托管渠道不再本机重复定时探测/报警；桌面读取服务器结果 |
| 后台独立运行 | 无浏览器时服务仍探测并投递邮件，服务重启保留去重状态 |
| 移动端 | 增加功能页导航，窄屏渠道行按网址/余额/操作分行显示 |
| 服务器界面 | 隐藏本机会话、Codex、扫描导入和切换本机供应商等不适用入口 |
| 桌面复用本机 Web | 校验本机凭据和同步协议，不再只判断端口及版本字符串 |
| 文档 | 替换过时路径、页面驱动调度说明和明文配置迁移说明 |

## 测试证据

- `pnpm test`：50 项通过，0 失败。覆盖探测换算、鉴权失败、Sub2API RT、渠道持久化、并发保存、邮件、账户、同步、会话数据库和 Codex 适配逻辑。
- `pnpm build`：TypeScript 与 Vite 构建成功。
- `pnpm lint`：退出码 0；仍有既有未使用代码、Fast Refresh 和供应商注入脚本警告，不作零警告声明。
- `scripts/verify-release.mjs`：两套真实 Node 服务，经过 HTTP 注册、登录、同步、冲突处理和重启；测试 SMTP 接收端实际收到三封分级邮件；未发送外部邮件。
- 同一验收覆盖模型列表、文本响应、生图返回结构；这些使用可控上游接口，未调用实际付费模型。
- Playwright Chrome：注册、登录、刷新恢复登录、退出、设置、手机导航、余额布局；无捕获到的页面运行错误。
- 最终 Web 证据：`artifacts/release-check-1789476521853/result.json` 和同目录截图。
- `pnpm exec tauri build`：Windows NSIS 安装包生成成功；Rust 中既有未使用代码警告保留。
- 安装到 `E:\Apps\BalanceRadar`，安装程序退出码 0；认证与同步模块的安装文件和源码 SHA-256 一致。
- 实际 WebView2 空数据启动：无需服务器登录；写入无效旧服务器首选项后重新加载仍进入本机工作区；可选同步表单正常。证据：`artifacts/desktop-ui-1789476657465/`。
- 恢复正常数据目录后验证 9 个渠道、10/6/1 阈值保留；本机 Web `http://127.0.0.1:8789` 已启动，桌面正常打开。

## 仍需目标环境验收

1. 本机 Docker Linux 引擎未启动，因此仅审查 Compose/Dockerfile，未实际构建和运行 Linux 容器。
2. 官方 Codex 没有可用浏览器 CDP 端口；插件安装、Timeline、会话移动等仅有隔离测试，未完成当前官方版本的界面实操。未修改或重启官方 Codex。
3. 真实上游付费文本/生图、实际云服务器域名/TLS和主机重启、外部邮箱投递需部署环境验收。
4. 同步渠道配置目前是显式点击；服务器托管渠道的余额结果由后台拉取。上游 RT/服务器同步会话过期后需要重连；服务器托管状态会显示同步错误，本机数据保留。
5. 全功能审查结果为“本轮新增主流程及既有自动化覆盖通过，目标环境项待验收”，不等于所有外部服务和所有 Codex 版本均已实测通过。

## 回退

本轮改造前源码备份：`artifacts/auth-sync-before-20260915/`。备份与测试产物均在 Git 忽略目录内。
新建账户文件后回退旧版会退回共享访问密码认证，回退前应停止公网访问并保留整个数据目录备份。数据库与配置不可只恢复单个加密 JSON 而遗漏密钥。
